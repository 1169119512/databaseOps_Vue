# 数据库连接批量导入说明

## 📋 方式一：使用界面导入（推荐）

### 操作步骤

1. **进入连接管理页面**
   - 访问前端：`http://localhost:80`
   - 点击左侧菜单"连接管理"

2. **点击批量操作**
   - 在页面顶部找到"批量操作"下拉按钮
   - 点击展开菜单

3. **选择批量导入**
   - 点击"批量导入子系统配置"
   - 系统会显示将要导入的13个子系统列表
   - 点击"导入"按钮确认

4. **导入完成**
   - 成功提示：显示导入的配置数量
   - 列表自动刷新，显示所有连接

### 功能说明

#### 批量操作菜单
```
[批量操作 ▼]
  ├─ 📤 批量导入子系统配置  ← 一键导入13个子系统
  ├─ 📥 导出当前配置        ← 导出为JSON文件
  └─ 🗑️ 清空所有连接       ← 危险操作，需二次确认
```

#### 导入的子系统列表
1. 供应商档案 (supplier-archive)
2. 发票池表头 (invoice-header)
3. 发票池明细 (invoice-detail)
4. 费用映射 (expense-mapping)
5. 客户分部配置 (customer-branch-config)
6. 法人主体配置 (legal-entity-config)
7. 用户管理 (user-management)
8. 税率配置 (tax-calculation-config)
9. 管理员配置 (admin-config)
10. 红字信息表主表 (red-letter-header)
11. 红字信息明细表 (red-letter-detail)
12. 角色管理 (role-management)
13. 项目管理 (project-management)

所有子系统的后端地址均为：`http://localhost:8080`

## 💻 方式二：使用浏览器控制台（高级）

### 操作步骤

1. **打开浏览器控制台**
   - 按 F12 打开开发者工具
   - 切换到 Console 标签

2. **执行导入命令**
   ```javascript
   // 导入子系统配置（与现有配置合并）
   localStorage.setItem('database_connections', JSON.stringify([
     {
       "id": Date.now() + "_0",
       "name": "供应商档案",
       "baseURL": "http://localhost:8080",
       "tableName": "supplier-archive",
       "description": "供应商档案管理子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_1",
       "name": "发票池表头",
       "baseURL": "http://localhost:8080",
       "tableName": "invoice-header",
       "description": "发票池表头管理子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_2",
       "name": "发票池明细",
       "baseURL": "http://localhost:8080",
       "tableName": "invoice-detail",
       "description": "发票池明细管理子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_3",
       "name": "费用映射",
       "baseURL": "http://localhost:8080",
       "tableName": "expense-mapping",
       "description": "费用映射配置子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_4",
       "name": "客户分部配置",
       "baseURL": "http://localhost:8080",
       "tableName": "customer-branch-config",
       "description": "客户分部配置子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_5",
       "name": "法人主体配置",
       "baseURL": "http://localhost:8080",
       "tableName": "legal-entity-config",
       "description": "法人主体配置子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_6",
       "name": "用户管理",
       "baseURL": "http://localhost:8080",
       "tableName": "user-management",
       "description": "用户管理子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_7",
       "name": "税率配置",
       "baseURL": "http://localhost:8080",
       "tableName": "tax-calculation-config",
       "description": "税率配置子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_8",
       "name": "管理员配置",
       "baseURL": "http://localhost:8080",
       "tableName": "admin-config",
       "description": "管理员配置子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_9",
       "name": "红字信息表主表",
       "baseURL": "http://localhost:8080",
       "tableName": "red-letter-header",
       "description": "红字信息表主表子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_10",
       "name": "红字信息明细表",
       "baseURL": "http://localhost:8080",
       "tableName": "red-letter-detail",
       "description": "红字信息明细表子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_11",
       "name": "角色管理",
       "baseURL": "http://localhost:8080",
       "tableName": "role-management",
       "description": "角色管理子系统",
       "createdAt": new Date().toISOString()
     },
     {
       "id": Date.now() + "_12",
       "name": "项目管理",
       "baseURL": "http://localhost:8080",
       "tableName": "project-management",
       "description": "项目管理子系统",
       "createdAt": new Date().toISOString()
     }
   ]))
   
   console.log('✅ 成功导入 13 个数据库连接配置')
   ```

3. **刷新页面**
   - 按 F5 刷新页面
   - 进入"连接管理"查看导入的配置

## ⚙️ 其他功能

### 导出配置
- 点击"批量操作" → "导出当前配置"
- 自动下载 JSON 文件
- 文件名格式：`database_connections_时间戳.json`

### 清空配置
- 点击"批量操作" → "清空所有连接"
- 需要二次确认
- ⚠️ 此操作不可恢复

## 📝 注意事项

1. **合并模式**
   - 批量导入采用合并模式
   - 不会删除现有的连接配置
   - 如果需要清空重建，请先使用"清空所有连接"

2. **端口配置**
   - 默认后端端口：`8080`
   - 如需修改，请在导入后手动编辑每个连接

3. **数据持久化**
   - 配置存储在浏览器 localStorage
   - 清除浏览器数据会丢失配置
   - 建议定期导出配置备份

## 🎯 快速验证

导入后，可以通过以下步骤验证：

1. 在"连接管理"页面查看列表
2. 点击任意连接的"测试"按钮
3. 点击"进入管理"查看数据

## ❓ 常见问题

**Q: 导入后看不到配置？**
A: 刷新页面（F5）即可

**Q: 可以导入其他配置吗？**
A: 可以，手动修改导入脚本中的配置数组

**Q: 如何修改后端地址？**
A: 导入后，在列表中点击"编辑"按钮修改

**Q: 配置会丢失吗？**
A: 存储在 localStorage，除非清除浏览器数据

